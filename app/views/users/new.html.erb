<% provide(:title, 'Sign Up') %>
<h1>Sign Up</h1>
<%# <p>Welcome new users! Join us by signing up here.</p> %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%# Call to form_with, with f as variable.) %>
    <%= form_with(model: @user, local: true) do |f| %> 
    <%# The ACTION of the form is (knows to) POST to URL /users(users path) %>
      <%# @user is an ActiveRecord object, @user is an argument form_with takes. %>
      <%= render 'shared/error_messages', object: f.object %>
      <%# ^^^ Renders a partial with a dedicated shared partial directory for views across multiple controllers. %>

      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>
      <%# form-control class added for each field (for formatting) %>

      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label :password %>
      <%= f.password_field :password, class: 'form-control' %>

      <%= f.label :password_confirmation, "Confirmation" %>
      <%= f.password_field :password_confirmation, class: 'form-control' %>

      <%= f.submit "Create my account", class: "btn btn-primary" %>
    <h5>PS: Don't worry about seeing an error message on the next page, and not receiving your signup confirmation email
immediately, we have prioritzed developing the learning materials you will be referencing during your learning and
interview processes! You will receive the email from us with your login info in the next few days, in the mean time,
start Googling around what software testers do!</h5>
    <% end %>
  </div>
</div>
